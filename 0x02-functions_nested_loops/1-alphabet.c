#include "main.h"
/**
 * print_alphabet - print alphabet in lower case
 * Return: 0 (success)
 */
void print_alphabet(void)
{
	char k;

	for (k = 'a'; k <= 'z'; k++)
		_putchar(k);
	_putchar('\n');
}
