#include "main.h"
/**
 * main - print alphabet in lower case
 * Return: 0 (success)
 */
int main(void)
{
	void print_alphabet(void);
	char k;

	for (k = 'a'; k <= 'z'; k++)
		_putchar(k);
	_putchar('\n');
}
